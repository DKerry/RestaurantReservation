<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ie.app.restaurantreservation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="ie.app.models.RestaurantReservation"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ie.app.activities.Login" />
        <activity android:name="ie.app.activities.Register" />
        <activity android:name="ie.app.activities.Options" />
        <activity android:name="ie.app.activities.ViewR" />
        <activity android:name="ie.app.activities.MakeR"></activity>
    </application>

</manifest>